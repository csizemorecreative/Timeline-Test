'use client';
import { createTheme } from '@mui/material/styles';
import { oclcDsBaseComponents, oclcDsBaseTokens } from './baseTheme';
import { typographySmall } from './components/typography';
import { spacing } from './components/spacing';
export const staffDefault = createTheme({
    ...oclcDsBaseTokens,
    typography: typographySmall,
    spacing: spacing,
    components: {
        ...oclcDsBaseComponents
    }
});
export default staffDefault;
