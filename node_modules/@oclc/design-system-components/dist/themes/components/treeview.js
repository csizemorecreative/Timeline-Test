import ChevronDown from 'mdi-material-ui/ChevronDown';
import ChevronRight from 'mdi-material-ui/ChevronRight';
import tokenData from '../../design-tokens/base-tokens.json';
export const MuiSimpleTreeView = {
    defaultProps: {
        itemChildrenIndentation: 0,
        slots: {
            collapseIcon: ChevronDown,
            expandIcon: ChevronRight
        }
    },
    styleOverrides: {
        root: {}
    }
};
export const MuiRichTreeView = {
    defaultProps: {
        itemChildrenIndentation: 0
    }
};
export const MuiTreeItem = {
    styleOverrides: {
        root: {},
        content: ({ theme }) => ({
            minHeight: 24,
            borderRadius: 0,
            gap: 2,
            paddingTop: theme.spacing(0.5),
            paddingBottom: theme.spacing(0.5),
            paddingLeft: `calc(var(--TreeView-itemChildrenIndentation) * var(--TreeView-itemDepth) + 4px)`,
            '&:hover, &[data-selected]:hover': {
                backgroundColor: tokenData.tokens.palette.content.content200
            },
            '&[data-selected]': {
                backgroundColor: tokenData.tokens.palette.blue.blue200,
                '& .MuiTreeItem-label': {
                    color: tokenData.tokens.palette.content.content900
                }
            },
            '&[data-selected][data-focused]': {
                backgroundColor: tokenData.tokens.palette.blue.blue200,
                '& .MuiTreeItem-label': {
                    color: tokenData.tokens.palette.content.content900
                }
            },
            '&[data-focused]': {
                backgroundColor: tokenData.tokens.palette.content.content200,
                outline: '2px solid #000000',
                outlineOffset: -2
            }
        }),
        groupTransition: {
            marginLeft: `calc(8px + var(--TreeView-itemChildrenIndentation) * var(--TreeView-itemDepth) + 4px)`,
            paddingLeft: `calc(6px + var(--TreeView-itemChildrenIndentation) * var(--TreeView-itemDepth))`,
            borderLeft: `0.5px solid ${tokenData.tokens.palette.content.content400}`
        },
        iconContainer: {
            color: tokenData.tokens.palette.content.content500
        },
        label: {
            color: tokenData.tokens.palette.content.content800
        }
    }
};
