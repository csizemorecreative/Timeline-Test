import { jsx as _jsx } from "react/jsx-runtime";
import tokenData from '../../design-tokens/base-tokens.json';
import Slide from '@mui/material/Slide';
function SlideTransition(props) {
    return _jsx(Slide, { ...props, direction: "right" });
}
export const MuiSnackbar = {
    defaultProps: {
        TransitionComponent: SlideTransition
    },
    styleOverrides: {
        root: {
            maxWidth: '344px'
        }
    }
};
export const MuiSnackbarContent = {
    styleOverrides: {
        root: ({ theme }) => ({
            backgroundColor: tokenData.tokens.palette.content.content800,
            borderRadius: '4px',
            minHeight: 36,
            paddingTop: 10,
            paddingBottom: 10,
            paddingLeft: 16,
            paddingRight: 12,
            ...theme.typography.body1
        }),
        message: {
            color: tokenData.tokens.palette.background.white,
            padding: 0
        },
        action: {
            '& .MuiButton-root': {
                color: tokenData.tokens.palette.blue.blue300,
                fontSize: '0.875rem'
            },
            '& .MuiIconButton-root': {
                color: tokenData.tokens.palette.background.white,
                '&:hover:not(.Mui-disabled), &.Mui-focusVisible': {
                    backgroundColor: 'transparent',
                    color: tokenData.tokens.palette.background.white,
                    outlineOffset: '2px',
                    outlineStyle: 'solid',
                    outlineWidth: '2px',
                    outlineColor: tokenData.tokens.palette.content.content900,
                    boxShadow: `0px 0px 0px 2px ${tokenData.tokens.palette.background.white}`
                }
            }
        }
    }
};
