export const spacingSmall = (value) => value > 0 ? (value * 4) + (2 * (0)) : 0;
export const spacing = (value) => value > 0 ? (value * 4) + (2 * (1)) : 0;
export const spacingLarge = (value) => value > 0 ? (value * 4) + (2 * (2)) : 0;
export const spacingSmallX = 4;
export const spacingX = 6;
export const spacingLargeX = 8;
export default spacing;
