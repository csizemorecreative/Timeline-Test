'use client';
import { createTheme } from '@mui/material/styles';
import { oclcDsBaseComponents, oclcDsBaseTokens } from './baseTheme';
import { typographySmall } from './components/typography';
import { spacingSmall } from './components/spacing';
export const staffCompact = createTheme({
    ...oclcDsBaseTokens,
    typography: typographySmall,
    spacing: spacingSmall,
    components: {
        ...oclcDsBaseComponents
    }
});
export default staffCompact;
