'use client';
import { createTheme } from '@mui/material/styles';
import { oclcDsBaseComponents, oclcDsBaseTokens } from './baseTheme';
import { typographySmall } from './components/typography';
import { spacingLarge } from './components/spacing';
export const endUser = createTheme({
    ...oclcDsBaseTokens,
    typography: typographySmall,
    spacing: spacingLarge,
    components: {
        ...oclcDsBaseComponents
    }
});
export default endUser;
